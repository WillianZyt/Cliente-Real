<form method="post" action="{{ path('app_admin_news_category_delete', {'id': news_category.id}) }}" onsubmit="return confirm('Quer mesmo excluir este item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ news_category.id) }}">
    <button class="btn btn-danger ms-3 mt-3 float-start text-white">Excluir</button>
</form>
